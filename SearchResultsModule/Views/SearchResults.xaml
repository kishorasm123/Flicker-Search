<UserControl x:Class="SearchResultsModule.Views.SearchResults"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SearchResultsModule"
             mc:Ignorable="d"
             xmlns:prism="http://prismlibrary.com/"
             d:DesignHeight="450" d:DesignWidth="800" prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/SearchResultsModule;component/Resources/ControlStyles.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Background="#5D5A58">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>

        <ListBox Background="#5D5A58" Grid.Row="0"  ItemsSource="{Binding SearchResult}" HorizontalContentAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Disabled"  BorderThickness="0">

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel></WrapPanel>
                    <!--<UniformGrid Columns="5" Rows="5" VerticalAlignment="Top" >
                        
                    </UniformGrid>-->
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" BorderBrush="Wheat" >
                        <VirtualizingStackPanel VirtualizationMode="Recycling">
                            <Image Source="{Binding Link}" Stretch="Uniform" MaxHeight="300" MaxWidth="300" ></Image>
                            <TextBlock Text="{Binding Title}"  MaxWidth="300" Foreground="Wheat" TextWrapping="WrapWithOverflow" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Bottom"></TextBlock>
                        </VirtualizingStackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Style="{StaticResource buttonStyle}" MinWidth="150" FontSize="25" Command="{Binding PreviousCommand}" >&lt;&lt; Previous Page</Button>
            <Separator Margin="10,0,10,0"></Separator>
            <Button  Style="{StaticResource buttonStyle}" MinWidth="150" FontSize="25" Command="{Binding NextCommand}">Next Page &gt;&gt;</Button>

        </StackPanel>

    </Grid>
</UserControl>
